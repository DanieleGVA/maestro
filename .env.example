# MAESTRO Development Environment
# Copy to .env and fill in values

# Database
POSTGRES_PASSWORD=changeme_dev

# Keycloak Admin
KEYCLOAK_ADMIN_PASSWORD=admin_dev

# LLM API Keys (optional for local dev -- content generation will use fallback)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# PII Encryption (dev-only key, NEVER use in production)
MAESTRO_PII_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef
